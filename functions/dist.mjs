import { distFor } from './partials/distFor.mjs'

/**
 * Simplified distribution tasks which will use arguments from distFor.
 * @memberOf module:js-build-tools
 * @returns {*}
 */
export const dist = () => distFor()
